{
  "name": "field-service-app-root",
  "version": "1.0.0",
  "description": "Root package for deployment",
  "scripts": {
    "render-build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server",
    "dev": "echo 'Please run client and server separately or install concurrently'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
